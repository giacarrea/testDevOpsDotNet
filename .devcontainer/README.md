# .devcontainer for C# (.NET)

This devcontainer uses the official .NET SDK 7.0 image and includes the C# extension.

To use:

- Open the workspace in VS Code.
- Run the Command Palette command: "Dev Containers: Reopen in Container".

You can also use the Dev Containers CLI:

```powershell
devcontainer up
```
